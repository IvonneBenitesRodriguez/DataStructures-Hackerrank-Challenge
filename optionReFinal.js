let originalArray = [1,2,3,4,5];
let newArray = [...originalArray].reverse();

console.log(newArray);

//Spread Operator:
//It is a syntax which allows an iterable element such an array or a string.
//It can be used when all elements from an object or array need to be included
// in a new array or object.